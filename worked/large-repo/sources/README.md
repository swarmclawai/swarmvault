# Large Repo Fixture

This is a tiny first-party markdown source so SwarmVault's markdown
extraction path has something to touch while the rest of the source tree
covers the remaining source classes.

The parent `worked/large-repo/README.md` explains how to run the worked
example end-to-end. This file exists purely so the ingest step sees
non-code first-party content too.
