# Tiny Validation Matrix

This fixture is the smallest repo-shaped corpus that still exercises every
shipped code language plus the non-code file kinds SwarmVault consumes during
local file and repo ingest.

It is intentionally tiny so installed-package validation stays cheap enough to
run regularly.
