# Durable Research Vaults

SwarmVault turns raw sources into durable markdown and graph artifacts.

Saved outputs in `wiki/outputs/` are meant to compound over time instead of disappearing into chat history.

The graph viewer and MCP server expose the same underlying vault in different ways.
